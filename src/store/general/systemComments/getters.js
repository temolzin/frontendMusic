export function stateComments(state) {
  return state.comments;
}
export function stateFilter(state) {
  return state.filter;
}
export function stateCanComment(state) {
  return state.canComment;
}
export function stateLoading(state) {
  return state.loading;
}
export function stateSubmitting(state) {
  return state.submitting;
}
